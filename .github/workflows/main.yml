on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp://4606679@f27-preview.runhosting.com/
        username: 4606679
        password: ${{ secrets.ftp_password }}
        server-dir: /home/www/schoolproject2.atwebpages.com
        local-dir: ./
